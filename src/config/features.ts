// Flags de funcionalidad fáciles de revertir.

// Panel de Audio (subir MP3/WAV, waveform, controles): deshabilitado
// temporalmente mientras se desarrolla más a fondo. Se reactiva en septiembre.
// Para reactivar: poner en `true`. El código real del panel queda intacto;
// esto solo controla la capa de bloqueo temporal en el editor.
export const AUDIO_FEATURE_ENABLED = false
